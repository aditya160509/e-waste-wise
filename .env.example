# Client (Vite)
# Required: Google Maps key used by CenterMap on the client.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# Optional: Override API base for the AI server in development.
# Defaults to http://localhost:8787 if not set in dev.
# VITE_API_BASE=http://localhost:8787

# Server (Node/Express)
# Required: OpenAI key (server-side only; do NOT prefix with VITE_)
OPENAI_API_KEY=your_openai_key_here

# Optional model overrides
OPENAI_MODEL_CHAT=gpt-4o
OPENAI_MODEL_CLASSIFY=gpt-4o-mini

# Optional: Express server port (defaults to 8787)
PORT=8787

